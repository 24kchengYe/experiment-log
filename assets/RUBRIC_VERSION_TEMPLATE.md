# Rubric version template moved

Use the maintained `RUBRIC_VERSION_TEMPLATE.md` from the independent `judge-rubric-engineering` skill.

This compatibility file intentionally contains no Rubric construction contract. In an Experiment Log, record only the consumed Rubric version, prompt/registry paths, manifest, hashes, and migration state.
